/** DTO (Data Transfer Object) that stores the info of a keyword in text format. */
export interface Keyword {
  /** Keyword name. */
  name: string;
  /** Keyword probability. */
  probability: number;
}

/** Environment variables with values used in production mode. */
export const environment = {
  /** If true, production mode is enabled. */
  production: true,
  /** Base url of the web backend. */
  baseUrl: 'https://topics-and-summary-web-backend.herokuapp.com'
};
